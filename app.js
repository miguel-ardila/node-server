// Importa la función initUI desde el archivo punto-de-entrada.js en la carpeta modules.
const { initUI } = require('./modules/punto-de-entrada');

// Ejecuta la función initUI que se importó anteriormente.
initUI();
